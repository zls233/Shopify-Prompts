`xx` 页面目前的实现与原页面仍有明显差异，没有达到预期。

请基于项目中已经保存的原页面 reference，以及当前实现页面，继续进行修改和精修。

不要重新设计页面，也不要按照自己的审美进行优化。原页面 reference 是本次修改和验收的主要标准，目标是尽可能准确地还原原页面的视觉效果、布局、响应式表现和交互行为。

先实际检查当前页面与 reference 的差异，重点关注：

* 页面整体布局与内容宽度
* Header、导航、公告栏等全局区域
* 各 section 的顺序、尺寸和位置
* 字体、字号、字重、行高、字间距
* margin、padding、gap 等间距
* 图片尺寸、比例、裁剪方式和对齐方式
* 背景色、边框、圆角、阴影
* 按钮样式和 hover / active 状态
* 商品卡片或内容卡片的结构
* Desktop 与 Mobile 的布局差异
* sticky、dropdown、carousel、tabs、accordion 等交互
* 页面滚动后的状态变化
* 当前实现中存在但原页面没有的元素
* 原页面存在但当前实现缺失的元素

发现差异后直接修改主题代码，不要只输出分析报告。

修改完成后，必须启动或访问实际 Shopify 主题预览，并使用浏览器或 Computer Use 对页面进行真实测试和验收。

至少检查：

* Desktop 页面
* Mobile 页面
* 首屏
* 页面中部主要 section
* 页面底部
* 主要交互元素

测试时将实际页面与 reference 进行逐项视觉对比。

如果仍然存在明显差异，不要停止任务，继续修改代码并重新测试。

按照以下循环执行：

检查 reference
→ 检查当前实现
→ 找出视觉 / 布局 / 交互差异
→ 修改代码
→ 在浏览器中实际查看
→ 再次与 reference 对比
→ 继续修正

不要在第一次修改后就结束。

只有在 Desktop 和 Mobile 的主要页面区域已经与 reference 基本一致，且主要交互正常后，才结束任务。

最终告诉我：

1. 修改了哪些主要内容
2. 测试了哪些页面尺寸和交互
3. 目前仍存在的无法完全还原的差异（如果有）

不要让我中途确认，也不要只给修改建议，直接完成修改、测试和验收。
