你现在需要继续完善当前 Shopify Theme 的商品详情页（Product Detail Page / PDP）。

当前商品详情页已经有基础结构，但仍然存在较多问题，例如：

* 页面与 reference / 原网站的实际布局差距较大
* 存在写死的商品介绍、卖点、评论或其他组件
* 当前商品数据没有完整驱动页面
* Variant / Color / Size 选择与图片、价格之间同步不完整
* 商品媒体展示逻辑不准确
* 部分信息使用静态内容代替真实 Shopify 数据
* Desktop / Mobile 的字体、间距、图片尺寸、布局比例仍然不够接近原页面

本次任务的目标不是重新设计一个类似的商品页。

目标是：

**根据现有 reference 文件和目标网站原商品详情页，对当前 Shopify 商品详情页进行逐项逆向分析和校准，在保持 Shopify 商品动态能力的前提下，尽可能做到结构、视觉、内容组织和交互行为与原页面 1:1 对齐。**

不要只进行局部 CSS 微调。

需要同时检查：

```text
Liquid
JSON template
Sections
Snippets
CSS
JavaScript
Shopify Product / Variant 数据
Metafields
Metaobjects
Media
URL / Variant 状态
Desktop / Mobile
```

---

# 一、参考来源

实现时必须同时参考：

1. 当前项目已有 reference：

```text
site-reference-summary.md
manifest.json
references/
Desktop screenshots
Mobile screenshots
SingleFile / WebArchive / HTML
页面结构说明
交互说明
```

2. 目标网站当前在线商品详情页。

3. 当前 Shopify 商品数据。

4. 当前主题已有实现。

如果当前实现与 reference / 原页面冲突：

**以 reference 和原网页实际表现为准。**

---

# 二、先做 PDP 逆向分析

不要看到当前页面后直接开始随意改 CSS。

先分别分析：

```text
Reference PDP
vs
当前 Shopify PDP
```

建立差异清单。

至少检查以下区域。

---

## 1. 页面整体结构

分析原页面 Product Page 的 Section 顺序。

例如：

```text
Breadcrumb
Product media
Product information
Reviews summary
Title
Subtitle
Price
Color
Variant selector
Size selector
Size guide
Quantity
Add to cart
Shipping / payment info
Accordion
Product description
Features
Materials
Guarantee
Reviews
Recommendations
Recently viewed
```

具体顺序以 reference 为准。

不要凭经验套标准 Shopify PDP。

---

# 三、商品首屏布局

优先把商品页 Above the Fold 做准确。

重点检查：

```text
页面最大宽度
左右栏比例
媒体区域宽度
Product Info 宽度
左右 gutter
顶部间距
Header 到商品主体距离
Sticky Product Info
Sticky Media
页面背景
```

测量 reference 的实际视觉比例，然后调整当前实现。

不要使用“看起来差不多”的固定数值。

---

# 四、Product Media Gallery

这是本次重点。

完整分析 reference 的商品媒体行为。

需要确认：

```text
Desktop 是单列、双列还是 Grid
Mobile 是横向 carousel 还是纵向列表
Thumbnail 是否存在
视频如何显示
媒体间距
图片比例
object-fit
图片圆角
导航箭头
pagination
zoom
```

然后按照 reference 实现。

---

# 五、颜色与媒体必须同步

当前商品存在多个 Color Variant 时：

用户选择颜色后，Product Media 必须切换到该颜色对应的媒体。

不能继续：

```text
54 张商品图片全部一起显示
```

而 reference 如果选择一个颜色后只显示该颜色对应的 8～12 张媒体，那么 Shopify 也应该做到相同逻辑。

优先根据以下数据建立 Variant → Media 对应关系：

```text
variant.featured_media
media.alt
variant option value
图片 alt
图片文件名
metafield
```

如果商品数据中已经存在可识别的颜色媒体关联，应自动解析。

例如：

```text
Color = Black
→ 只显示 Black 对应 media

Color = Blue
→ 切换成 Blue 对应 media
```

不要把媒体 ID 写死。

---

# 六、商品标题与副标题

商品标题必须使用：

```liquid
product.title
```

不要写死。

如果原页面存在副标题，例如：

```text
Men's lightweight insulated jacket
```

优先查找：

```text
product.metafields
product.type
product.vendor
product.description
```

是否存在对应数据。

如果确实需要新 metafield，可以定义：

```text
custom.subtitle
```

不要直接把某个商品的副标题写进 Liquid。

---

# 七、Reviews Summary

如果 reference 在商品标题附近存在：

```text
★★★★★ 4.8 (127 Reviews)
```

需要实现相同视觉结构和跳转行为。

但禁止写死：

```text
4.8
127 reviews
```

如果当前没有真实 Reviews App 数据：

优先：

1. 查找主题是否已有 Reviews App block。
2. 查找 metafield。
3. 提供 App Block 插槽。
4. 没数据时隐藏。

不要制造假评论。

点击评分区域时，如果 reference 会滚动到 Reviews Section，应实现对应 anchor：

```text
#reviews
```

---

# 八、价格

价格必须由当前 Variant 动态驱动：

```liquid
variant.price
variant.compare_at_price
```

检查 reference 是否存在：

```text
Sale price
Compare at price
Discount badge
Percentage discount
Installment info
```

如果存在折扣，显示方式需要与 reference 一致。

选择 Variant 后：

```text
价格
compare-at-price
折扣信息
Add to Cart 按钮价格
```

必须一起更新。

禁止页面显示一个价格、购物车提交另一个价格。

---

# 九、Color Selector

分析 reference 中 Color Selector 的：

```text
位置
标题
当前颜色名称
Swatch 尺寸
圆形 / 方形
边框
选中状态
hover
不可用状态
间距
```

例如：

```text
Color: Black
```

颜色名称必须随着 Variant 更新。

不要只显示一排色块却没有当前颜色名称。

---

# 十、Swatch 颜色

优先使用 Shopify 原生：

```text
swatch
variant option value
metafield
```

其次使用项目已有颜色映射。

例如：

```text
Black → #000000
Navy → #...
```

支持：

```text
纯色
双色
渐变
图片 swatch
```

如果当前项目已经有：

```css
linear-gradient(135deg, ...)
```

之类的双色颜色实现，继续复用，不要重新做另一套。

---

# 十一、Size / Variant Selector

分析 reference：

```text
Button
Dropdown
Pills
Radio
```

具体使用哪一种。

需要支持：

```text
available
selected
sold out
disabled
unavailable combination
```

不可用 Variant 必须视觉上明确。

禁止用户选择 Shopify 中不存在的 Variant Combination。

---

# 十二、Variant 状态必须真正同步

Color / Size 等选项变化后必须：

```text
重新计算 selected variant
更新 URL variant 参数
更新价格
更新 compare price
更新图片
更新库存状态
更新 Add to Cart 状态
更新 button text
更新 SKU（如果页面显示）
```

例如：

```text
/products/example?variant=123456
```

必须与当前选中的 Variant 一致。

页面刷新后也应恢复正确 Variant。

---

# 十三、Add to Cart

Add to Cart 必须提交真实：

```text
variant.id
quantity
```

不要写死 product ID。

分析 reference 的按钮：

```text
高度
宽度
边框
颜色
font
letter spacing
hover
disabled
loading
```

按钮文本可能是：

```text
Add to Cart
Add to Bag
Sold Out
Unavailable
```

具体以 reference 为准。

如果 reference 按钮同时显示价格：

```text
Add to Bag — $85
```

则需要动态同步。

---

# 十四、商品描述

禁止写死：

```text
某个具体商品的介绍
材质
特点
技术参数
```

所有商品内容优先来自：

```liquid
product.description
```

或者：

```text
product.metafields
```

如果原页面结构化展示：

```text
Description
Features
Materials
Care
Fit
Details
```

则优先创建结构化 metafields。

例如：

```text
custom.features
custom.materials
custom.care
custom.fit
```

没有数据时：

```text
隐藏对应模块
```

不要显示其他商品的内容。

---

# 十五、Accordion

如果 reference 使用 Accordion：

严格复刻：

```text
标题
上下 padding
border
icon
展开动画
字体
默认状态
```

使用：

```html
button
```

作为 trigger。

不要使用：

```html
<a href="#">
```

实现 Accordion。

需要支持键盘操作与：

```text
aria-expanded
aria-controls
```

---

# 十六、Guarantee / Shipping / Returns

如果原页面存在：

```text
Lifetime Guarantee
Guaranteed for Good
Free Shipping
Returns
Warranty
```

不要直接写死到每个商品。

先判断这些内容属于：

```text
全局品牌内容
商品 metafield
Theme section
Shopify Page
Metaobject
```

全局内容可以通过 Section Settings / Metaobjects 管理。

如果有：

```text
Learn More
Warranty
Returns
```

链接必须指向真实 Shopify Page。

禁止：

```html
href="#"
```

---

# 十七、动态 Section / App Block

商品页应保留 Shopify 的动态扩展能力。

对于：

```text
Reviews
Subscriptions
Upsell
Size guide
Payment
Apps
```

优先使用：

```liquid
{% content_for 'blocks' %}
```

或当前主题兼容的 App Block 机制。

不要把第三方功能写死。

---

# 十八、Reviews Section

如果 reference 有完整 Reviews 区域：

先分析原页面的：

```text
总评分
评分分布
Review card
用户名
日期
图片
Pagination
Filter
Sort
```

如果当前 Shopify 没有真实评论数据：

不要伪造评论。

只实现：

```text
Reviews App slot
对应布局容器
正确 anchor
正确 spacing
```

有真实 App 后由 App 输出实际数据。

---

# 十九、Recommendations

如果 reference 有：

```text
You May Also Like
Recommended For You
Complete The Look
```

优先使用 Shopify：

```text
Product Recommendations API
```

不要写死具体商品 Handle。

商品卡需要复用已有：

```text
product-card
```

并保证：

```text
颜色
价格
hover image
链接
折扣
```

都正常。

---

# 二十、不要写死商品业务内容

全文搜索商品页代码中的静态业务内容。

特别检查：

```text
某个商品名
某个具体价格
某个具体颜色
某个具体 SKU
某段商品描述
某个固定 review
固定 rating
固定图片 URL
固定 variant id
```

如果属于商品数据：

必须改为 Shopify 动态数据。

---

# 二十一、图片与其他资源

商品图片优先使用 Shopify Product Media。

不要在商品模板里硬编码某个商品的 CDN 图片 URL。

例如禁止：

```liquid
<img src="https://...specific-product-image.jpg">
```

应通过：

```liquid
product.media
variant.featured_media
image_tag
```

输出。

---

# 二十二、Desktop 视觉校准

使用 reference 的 Desktop Screenshot 与原网页逐项比较。

重点检查：

```text
Product media width
Product info width
Title size
Price size
Color label
Swatch size
Variant buttons
CTA height
Section spacing
Accordion height
Borders
Image gap
Content max-width
```

不要仅凭肉眼。

可以使用 Playwright 获取元素：

```text
boundingBox
computedStyle
font-size
font-weight
line-height
gap
padding
margin
width
height
```

对 source 与 Shopify 页面进行比较。

---

# 二十三、Mobile 单独校准

Mobile 不允许简单变成：

```text
Desktop flex-direction: column
```

必须参考 Mobile Screenshot。

检查：

```text
媒体 carousel
图片 viewport 宽度
左右 padding
标题位置
价格位置
Variant selector
Color swatch
CTA
Sticky Add to Cart
Accordion
Recommendations
```

如果 source Mobile 和 Desktop 交互不同，分别实现。

---

# 二十四、Sticky 行为

如果 reference Desktop 的 Product Info 是 Sticky：

实现相同：

```css
position: sticky;
top: ...
```

但必须确认：

```text
Header sticky 高度
Announcement bar
Navigation
Product section container
```

避免 Product Info 被 Header 遮挡。

如果 source Mobile 不 sticky，则 Mobile 不要照搬。

---

# 二十五、JavaScript 原则

尽量使用简洁的原生 JavaScript。

不要因为一个 Variant Selector 引入大型框架。

JS 主要负责：

```text
Variant state
Media switching
URL state
Price update
Availability
Button state
Accordion
Gallery
```

HTML / Liquid 负责输出初始正确状态。

即使 JS 加载稍慢，页面首屏也不应完全错误。

---

# 二十六、Accessibility

保持：

```text
button
aria-label
aria-expanded
aria-selected
disabled
alt
keyboard navigation
focus state
```

尤其是：

```text
Color swatches
Variant selectors
Media carousel
Accordion
```

不要为了复刻视觉牺牲基本可访问性。

---

# 二十七、不要破坏其他页面

修改 PDP 时：

不要影响：

```text
Collection product card
Home product card
Cart
Header
Footer
Search
```

如果需要修改全局：

```text
theme.css
theme.js
product-card
```

必须先判断影响范围。

---

# 二十八、测试商品

不要只测试一个 Product。

至少选择：

```text
1 个多颜色、多尺寸商品
1 个颜色较少商品
1 个存在 sold-out variant 的商品（如果有）
```

验证动态逻辑。

---

# 二十九、实际浏览器验收

实现完成后必须启动 Shopify Theme Preview，然后使用浏览器 / Playwright 对 reference 与 Shopify PDP 进行实际比较。

至少测试：

### Desktop

```text
1440px
```

### Mobile

```text
390 × 844
```

如果 reference 的采集尺寸不同，则优先使用 reference 的尺寸。

---

# 三十、交互验收

实际点击测试：

```text
Color 1
Color 2
Size 1
Size 2
Sold out option
Add to cart
Accordion
Reviews anchor
Recommendations
```

确认：

```text
图片正确切换
Variant 正确
URL 正确
价格正确
库存正确
按钮正确
购物车加入的 SKU 正确
```

不要只读取 DOM 判断功能正常。

---

# 三十一、截图对比

对以下状态截图：

```text
Desktop 首屏
Desktop 页面中部
Mobile 首屏
Mobile Variant selector
Color A
Color B
```

和 reference 对比。

优先修复视觉误差最大的项目。

建议按以下顺序：

```text
1. 页面整体宽度与比例
2. Product Media
3. Product Info 位置
4. 字体
5. 间距
6. Variant controls
7. CTA
8. Accordion
9. 下方 Sections
10. 细节
```

不要一开始花大量时间修 1px border，而整体布局仍然不正确。

---

# 三十二、代码位置

优先检查当前主题中已有：

```text
sections/main-product.liquid
sections/product-reviews.liquid
snippets/product-*.liquid
assets/theme.css
assets/theme.js
templates/product.json
```

具体文件以当前仓库实际结构为准。

不要因为文件名与这里不同就额外创建一套重复组件。

---

# 三十三、禁止事项

禁止通过以下方式“完成”：

```text
写死一个商品内容让截图看起来正确
写死评论
写死 Variant
写死价格
写死图片
所有 Color 都展示相同图片
隐藏真正有问题的组件
使用 href="#"
用首页 fallback
大量 absolute positioning 拼截图
只修 Desktop 不修 Mobile
只运行 theme check 不打开页面
```

---

# 三十四、完成标准

只有以下条件同时满足，才认为商品详情页完成：

* Product 信息完全由 Shopify 数据动态驱动。
* 不存在与当前 Product 无关的写死内容。
* 不存在假评论。
* Color / Size Variant 可以正常操作。
* Variant Combination 正确。
* Color 切换会显示对应颜色的 Product Media。
* Product Media 不再无差别展示所有颜色图片。
* Variant 切换同步 URL。
* Variant 切换同步价格。
* Variant 切换同步库存状态。
* Add to Cart 提交正确 Variant。
* Sold Out / Unavailable 状态正常。
* Product Description / Features / Materials 等使用动态数据。
* Reviews 有真实数据源或 App Block，没有假数据。
* 所有链接真实有效。
* Desktop 布局接近 reference。
* Mobile 布局接近 reference。
* Header / Footer / Collection 等其他页面未被破坏。
* `shopify theme check` 通过，或剩余 warning 已明确说明。
* 已使用真实浏览器完成交互验收。

---

# 三十五、最终汇报

完成后不要只说：

```text
PDP 已完成
```

给出实际修改摘要：

```text
1. 修改了哪些文件
2. PDP 原本有哪些主要问题
3. Product Media 如何重新实现
4. Color → Media 的匹配逻辑是什么
5. Variant state 如何管理
6. 哪些写死内容被删除
7. Product description / metafield 使用了哪些数据
8. Reviews 如何处理
9. Desktop 做了哪些校准
10. Mobile 做了哪些校准
11. 实际测试了哪些 Product URL
12. 测试了哪些 Variant
13. Add to Cart 是否实际验证
14. 当前还存在哪些无法完成的差异
```

对于仍然存在的差异，明确说明：

```text
source 表现
当前表现
原因
建议下一步
```

不要隐藏未完成项。
