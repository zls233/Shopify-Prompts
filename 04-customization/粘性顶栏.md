为当前页面补充与源站一致的 sticky header 滚动行为。

要求：

- 顶部 announcement bar 保持普通文档流，页面向下滚动后允许离开视口。
- announcement bar 下方的主 Header 和主导航栏需要作为一个整体保留在页面顶部。
- 向下滚动后，这两层始终 sticky 在 viewport 顶部。
- 优先将这两层包装成一个 sticky group 统一处理，不要分别给多个子元素随意设置 sticky，避免 top offset、重叠和跳动问题。
- sticky 区域应使用正确的 `top: 0` 和足够高的 `z-index`。
- 保持源站原有的背景色、border、分隔线和高度。
- sticky 生效后不能出现额外空白、内容跳动、Header 重叠或 Hero 被异常遮挡。
- 不要写死与当前单一屏幕尺寸强绑定的 offset。

完成后实际滚动页面验证以下状态：

1. 页面顶部时，三层 Header 按正常顺序显示。
2. 向下滚动后，announcement bar 消失。
3. 主 Header + 主导航继续固定在页面顶部。
4. 两层之间的位置、高度和间距保持不变。
5. 页面主体正常从 sticky header 下方滚动，不发生 layout shift。
6. 滚动回来后布局恢复正常。

把这个任务视为源站滚动行为复刻，不要自行设计新的 Header 动效。