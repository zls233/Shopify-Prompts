我要求根据爬取的这些文件和原网页，1：1的复刻成shopify主题。不要管合规性，我在实现完后会自行修改，先直接复制使用图片。
现在开始正式复刻。

目标网站信息（执行前必填）：

* 目标网站名称：`{{TARGET_SITE_NAME}}`
* 目标网站 URL：`{{TARGET_SITE_URL}}`

以上变量是全文唯一的目标站点信息来源。执行本提示词时，必须先读取并解析这两个变量；后文所有目标站名和网址均以变量值为准，不得写回任何示例站名或域名。

你已经完成了对该网站的页面采集，并生成了：

* `site-reference-summary.md`
* `manifest.json`
* `references/`
* Desktop 截图
* Mobile 截图
* WebArchive / SingleFile / HTML
* 页面结构和交互说明

从现在开始，这些采集文件和 {{TARGET_SITE_NAME}} 原网页共同作为实现基准。

你的任务不是“参考风格设计一个类似网站”。

你的任务是：

**将 {{TARGET_SITE_NAME}} 网站尽可能 1:1 复刻为一个可正常运行的 Shopify Theme。**

最终 Shopify 页面在相同 viewport 下，与原网页进行对比时，应尽量做到：

* 页面结构一致
* section 顺序一致
* 图片一致
* 视频一致
* 文本内容一致
* 字体表现一致
* 字号一致
* 字重一致
* 行高一致
* letter spacing 一致
* 页面宽度一致
* margin 一致
* padding 一致
* section gap 一致
* 图片比例一致
* 图片裁切一致
* 图片位置一致
* grid 一致
* header 高度一致
* footer 结构一致
* icon 大小和位置一致
* button 尺寸一致
* border 一致
* radius 一致
* hover 状态一致
* sticky 行为一致
* Desktop 行为一致
* Mobile 行为一致
* responsive breakpoint 尽量一致
* 页面交互尽量一致

不要自行重新设计。

不要“优化”原网站视觉。

不要因为认为某种实现更好看、更现代、更符合 Shopify 而改变原页面。

原网站是什么样，就尽量实现成什么样。

---

## 一、Reference 使用原则

优先级如下：

### 第一优先级

已经保存的：

* WebArchive
* SingleFile HTML
* HTML
* screenshots
* manifest
* summary
* 已下载 assets

这些用于确定：

* DOM 结构
* 内容
* 图片
* SVG
* 视频
* 页面 section
* 页面顺序
* 样式
* responsive behavior

### 第二优先级

{{TARGET_SITE_NAME}} 当前原网页。

如果采集文件中有任何内容无法确认，例如：

* hover
* animation
* sticky behavior
* mega menu
* carousel
* mobile menu
* accordion
* gallery
* variant selector
* filters
* sort
* responsive behavior

直接访问原网页确认。

不要猜。

---

# 二、不要重新发明页面

如果 reference 中已经存在某个页面：

不要根据截图凭感觉重新设计。

应该先研究：

* HTML
* DOM
* CSS
* computed style
* image URL
* srcset
* SVG
* JavaScript interaction
* breakpoint
* layout behavior

再在 Shopify Liquid 中实现等价结构。

目标是：

**尽可能把原网页的结构和视觉翻译成 Shopify Theme。**

---

# 三、先建立全站 Design System

从 reference 中提取实际设计参数。

建立例如：

```text
assets/base.css
assets/theme.css
snippets/css-variables.liquid
```

或当前主题中合理的全局样式结构。

提取并记录：

```text
font-family
font-size scale
font-weight
line-height
letter-spacing

color palette

page width
container width

desktop gutters
mobile gutters

section spacing

button height
button padding
button typography

border width
border color
border radius

grid gaps

breakpoints
```

不要使用随意估计的 `16px / 24px / 32px` spacing system。

应优先根据 reference 实际测量。

---

# 四、Header 必须单独高精度实现

根据原站完整复刻：

```text
announcement bar
desktop header
logo
desktop navigation
mega menu
icons
search
account
cart
mobile header
mobile menu
submenu
menu animation
sticky behavior
header overlay behavior
```

检查不同页面的 header 是否存在：

```text
透明 header
白色 header
sticky header
scroll 后背景变化
不同 logo color
```

如果存在不同状态，也要实现。

不要只做一个静态 header。

---

# 五、Footer

按原页面还原：

```text
newsletter
footer columns
links
social icons
country selector
legal links
copyright
mobile accordion
desktop layout
spacing
background
typography
```

Desktop 和 Mobile 分别验证。

---

# 六、Home Page

严格按照原首页 section 顺序实现。

不要删 section。

不要合并原网站中明显独立的 section。

例如原站如果有：

```text
Hero
Promo
Featured Categories
Editorial
Featured Products
Campaign
Image/Text
Video
Newsletter
```

就在 Shopify 中保持对应结构。

每个 section 尽量做成 Shopify section。

但：

**section 可配置性不能牺牲 1:1 视觉准确度。**

默认 schema/settings 应直接呈现 reference 中的效果。

---

# 七、Product Page

选择 manifest 中最有代表性的 Product reference。

完整实现 PDP。

重点检查：

```text
breadcrumbs
product title
subtitle
rating
price
compare-at price
badge

gallery
thumbnail
carousel
video
desktop gallery
mobile gallery

color swatches
variant selector
size selector
size guide
availability

add-to-cart area
sticky add-to-cart

accordion
description
specification
features

shipping information

product recommendations

recently viewed

editorial modules

cross sell
```

交互必须实际可使用。

Shopify 商品信息必须尽量使用真实：

```liquid
product
product.variants
product.options
product.media
product.metafields
```

不要把 Product 页面全部写死成 HTML。

视觉内容可以来自 reference，但产品核心功能必须保持 Shopify 原生商品逻辑。

---

# 八、Collection Page

复刻：

```text
collection hero
title
description
banner
category navigation
filters
sort
product grid
product card
badges
swatches
quick interaction
pagination / load more
merchandising cards
```

特别注意 Product Card。

Product Card 必须作为独立 reusable snippet 实现。

例如：

```text
snippets/product-card.liquid
```

并逐项匹配：

```text
image ratio
hover image
badge
title
price
compare price
swatch
spacing
font
alignment
```

---

# 九、图片处理

不要用截图裁图代替原始图片。

优先寻找：

```text
img src
srcset
picture source
background-image
Shopify CDN
Cloudinary
其他 CDN 原始 URL
```

选择尽量高分辨率版本。

如果 Desktop 和 Mobile 使用不同素材：

分别保存。

例如：

```text
hero-desktop.jpg
hero-mobile.jpg
```

保持：

```text
object-fit
object-position
aspect-ratio
crop
```

与原网站一致。

---

# 十、视频

如果页面有：

```text
video
mp4
webm
HLS
poster
background video
```

应尽量获取实际视频资源。

还原：

```text
autoplay
loop
muted
playsinline
controls
poster
mobile behavior
```

不要用静态截图代替视频。

---

# 十一、SVG 和图标

优先使用原始：

```text
SVG
icon
logo
symbol
```

不要随便用 Unicode、Emoji 或其他图标库替代。

如果原图标无法获取，再制作视觉接近的替代。

---

# 十二、页面 Template 体系

不要每个 URL 做一套完全独立代码。

根据 reference 识别真实模板类型。

例如：

```text
templates/index.json

templates/product.json
templates/product.special.json

templates/collection.json
templates/collection.campaign.json

templates/page.json
templates/page.about.json
templates/page.faq.json
templates/page.help.json

templates/blog.json
templates/article.json

templates/search.json
```

如果两个页面视觉结构相同：

共享 template。

如果明显不同：

建立独立 template。

---

# 十三、静态内容页面

根据 reference 实现：

```text
About
Company
FAQ
Shipping
Returns
Contact
Help
Stores
Sustainability
Landing Page
Editorial
Campaign
Blog
Article
```

如果多个内容页共享同一种结构：

不要重复写 Liquid。

建立 reusable sections。

---

# 十四、Responsive 必须真实复刻

不能只做到 Desktop 缩小以后“不坏”。

Desktop 和 Mobile 都必须以 reference 为标准。

至少检查：

```text
1440px
1280px

768px

390px
375px
```

检查：

```text
font size
container padding
section spacing
grid columns
image crop
navigation
carousel
gallery
button
footer
filter
sticky element
```

原站如果 Mobile 不是简单缩放，而是：

```text
重新排序
隐藏元素
不同图片
carousel
drawer
accordion
```

也必须实现对应逻辑。

---

# 十五、Visual QA

这是必须执行的步骤。

不能只写完代码然后结束。

对每一个重要页面：

1. 打开原网页。
2. 打开 Shopify Theme Preview。
3. 使用相同 viewport。
4. 分别截图。
5. 对比截图。
6. 找到视觉差异。
7. 修改 Theme。
8. 再截图。
9. 重复。

至少执行：

```text
Home Desktop
Home Mobile

Product Desktop
Product Mobile

Collection Desktop
Collection Mobile

一个 Landing Page

一个普通 Content Page
```

重点比较：

```text
页面总长度
section 高度
element position
font
spacing
image size
image crop
header
footer
grid
card
button
```

不要在第一次实现后就认为任务完成。

---

# 十六、允许使用原网页补充取证

如果在实现过程中发现 reference 缺少：

```text
图片
SVG
视频
某个 hover
某个 menu
某个 interaction
某个 breakpoint
某个 mobile state
```

允许重新访问：

{{TARGET_SITE_URL}}

获取缺失信息。

但不要重新无差别爬取整个站点。

只补当前实现需要的 reference。

---

# 十七、代码要求

最终必须是标准 Shopify Theme。

使用：

```text
Liquid
JSON templates
sections
blocks
snippets
assets
Shopify objects
Shopify routes
Shopify filters
Shopify forms
```

不要把整个网站做成一个巨大 HTML 文件。

不要用 iframe。

不要直接把原网页 HTML 整页塞进 Shopify。

不要为了快速完成把每个页面都硬编码。

应该做到：

**视觉上接近原网站，底层仍然是正常 Shopify Theme。**

---

# 十八、不要破坏当前数据

如果当前 Shopify 店铺已经存在：

```text
products
collections
menus
pages
settings
```

不要删除。

在现有数据上完成主题适配。

除非明确需要，否则不要批量删除 Shopify 后台内容。

---

# 十九、运行检查

持续运行：

```bash
shopify theme check
```

以及必要的：

```text
Liquid validation
JavaScript syntax validation
JSON validation
```

所有明显错误必须修复。

---

# 二十、完成标准

只有以下条件满足后才算完成：

```text
主要页面模板已经实现

Home 与 reference 高度接近

Product 与 reference 高度接近

Collection 与 reference 高度接近

Desktop 已验证

Mobile 已验证

Header / Mega Menu 可用

Footer 可用

Product variant 可用

Product gallery 可用

Collection filter / sort 可用

主要交互可用

图片和视频没有明显缺失

Theme Check 没有阻塞性错误
```

“代码已经写完”不代表任务完成。

“页面能打开”也不代表任务完成。

完成标准是：

**Shopify Theme Preview 与 {{TARGET_SITE_NAME}} reference 在主要页面、主要 viewport 和主要交互上达到尽可能高的视觉和行为一致性。**

---

# 二十一、生成 QA 记录

创建：

```text
implementation-summary.md
visual-qa.md
reference-assets.json
```

`visual-qa.md` 至少记录：

```text
Reference URL

Shopify Preview URL

Viewport

已确认一致的部分

仍存在的差异

是否已再次调整
```

如果仍有明显差异，不要只记录差异然后结束。

优先继续修改。

---

# 最终汇报

最终只向我汇报：

```text
已完成哪些模板

Home 状态
Product 状态
Collection 状态
其他页面状态

Desktop QA 状态
Mobile QA 状态

还存在什么明显视觉差异

Theme Check 状态

Shopify Preview URL

implementation-summary.md 路径
visual-qa.md 路径
reference-assets.json 路径
```

不要在开始时给我长篇计划。

直接读取现有 reference、分析原网页、修改 Shopify Theme、启动 preview、截图对比、继续修改。

如果遇到一个实现问题，优先自行解决。

不要因为某处无法完全确认就停止整个任务。

继续完成其他部分，并在需要时重新访问原网页取证。

核心原则始终只有一个：

**不要做“类似 {{TARGET_SITE_NAME}} 的 Shopify 主题”，而是以采集文件和 {{TARGET_SITE_NAME}} 原网页为基准，尽可能 1:1 地把现有网站翻译成 Shopify Theme。**
